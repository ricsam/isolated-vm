{
  "name": "@ricsam/isolated-vm",
  "version": "6.1.3",
  "description": "Access to multiple isolates",
  "main": "isolated-vm.js",
  "types": "isolated-vm.d.ts",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "install": "node-gyp-build || node-gyp rebuild --release -j max",
    "prebuild": "node scripts/run-prebuild.js",
    "rebuild": "node-gyp rebuild --release -j max",
    "lint": "find src -name '*.cc' | xargs -n1 clang-tidy",
    "test": "node test.js"
  },
  "dependencies": {
    "node-gyp-build": "^4.8.4"
  },
  "devDependencies": {
    "@ricsam/isolated-vm": ".",
    "prebuildify": "^6.0.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ricsam/isolate.git"
  },
  "author": "ricsam <oss@ricsam.dev>",
  "license": "ISC",
  "gypfile": true,
  "bugs": {
    "url": "https://github.com/ricsam/isolated-vm/issues"
  },
  "homepage": "https://github.com/ricsam/isolated-vm#readme"
}
